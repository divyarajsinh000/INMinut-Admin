# Public API origin only. Do not append /api because the Axios client adds it
# automatically to non-admin routes. Admin routes remain under /admin.
# Never put passwords, JWT secrets, database URLs, Firebase private keys,
# AWS secret keys, or service-account JSON in VITE_* variables.
VITE_API_URL=https://api.inminut.com
